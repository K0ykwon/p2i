# Explorer validation — 2026-09-23

Baseline: upstream `main` at `7720d4d49d9e57c82c40b1f9fa2a3674fe83a021`, including the contributed node-dragging feature. Before changes: **102 Python tests passed**, production frontend build passed.

Local verification environment: Linux x86-64, Python 3.12.14, PyTorch 2.6.0+cpu, Node 24.19.0, Chromium 134.0.6998.35. CI config additionally targets Python 3.11 and uses Node 22. Local results do not establish a successful hosted CI run.

| Check | Result |
|---|---|
| Full `python -m pytest -q` | 109 passed, 1 skipped; one upstream Starlette deprecation warning |
| CUDA trace smoke | Skipped: CUDA unavailable |
| `npm ci` and production TypeScript/Vite build | Passed |
| Frontend pure-function tests | 3 passed: runtime ordering, row-major capture bounds, shuffled DAG layering |
| Existing browser scenarios | All 8 passed: legacy explorer, 3 Phase 2 demos, Phase 3 skill flow, 3 progressive UX demos; the latter also verify bounded whole-tensor grids, no default raw prefix storage and aligned input/output layout |
| New microscope browser scenario | Passed: playback/pause/restart, reduced motion, drag persistence, whole-tensor heatmaps, actual before/after edit and synchronized operation/tensor inspection |
| Large-graph browser scenario | Passed: actual 500/1,000/3,000-operation traces, local graph bounded to ≤7 operations, next-step response recorded |
| Distribution | Wheel and source distribution built with frontend assets |
| Clean wheel environment | Installed with CPU PyTorch; all 3 CLI demos, API and packaged frontend assets passed without Node |

Python comparison regression checks ensure a failed retrace does not replace successful snapshots and transient operation IDs do not define cross-trace equivalence. Study recorder tests preserve ungraded correctness as null and validate confidence/category metadata.

Performance JSON is generated by `scripts/benchmark_explorer.py` and the browser acceptance runner. Earlier captured IR sizes were approximately 285 KB, 570 KB and 1.73 MB for 500, 1,000 and 3,000 sine operations; earlier sidecar sizes measured the former prefix-capture format and must not be reused as measurements of the new whole-tensor mean grids. Single-pass trace and browser timings vary substantially; inspect newly generated environment-labelled JSON and repeat measurements before any paper claim. No human study or CUDA performance claim is made.

Limits: simple DAG layering rather than optimal edge-crossing minimization; higher-rank tensors average leading axes and pool the last two into a bounded rectangular grid, so individual slices and full-resolution values are unavailable; capture is opt-in and capped at 64 CPU tensors of at most 262,144 elements each; raw graphs require intentional expansion; before/after matching is structural and can be ambiguous for repeated identical operators. Direct tensor intervention, automatic causal attribution and autonomous research agents remain unimplemented.
